"use client"; import {useState} from "react"; export default function Page(){const [data,setData]=useState<any>(null);async function load(){const r=await fetch("/api/weather?lat=27.99&lng=86.93");setData(await r.json())}return <section className="section"><div className="container"><div className="pagehero"><div className="eyebrow">LIVE DATA</div><h1>Weather & conditions</h1><p className="muted">The app is wired to a live forecast endpoint. Always verify official advisories and local guide reports before travel.</p><button className="btn" onClick={load}>Load Everest-area forecast</button></div>{data&&<pre className="chat">{JSON.stringify(data,null,2)}</pre>}</div></section>}