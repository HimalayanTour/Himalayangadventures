import {NextResponse} from "next/server";
export async function GET(req:Request){const u=new URL(req.url);const lat=u.searchParams.get("lat")||"27.9881";const lng=u.searchParams.get("lng")||"86.9250";const url=`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lng}&current=temperature_2m,precipitation,wind_speed_10m,weather_code&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max,weather_code&timezone=auto&forecast_days=7`;try{const r=await fetch(url,{cache:"no-store"});return NextResponse.json(await r.json())}catch{return NextResponse.json({error:"Weather service unavailable"},{status:502})}}
