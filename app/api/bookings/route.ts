import {NextResponse} from "next/server";
export async function POST(req:Request){try{const body=await req.json();if(!body.name||!body.email)return NextResponse.json({error:"Name and email are required"},{status:400});if(!process.env.NEXT_PUBLIC_SUPABASE_URL||!process.env.SUPABASE_SERVICE_ROLE_KEY)return NextResponse.json({message:"Booking request received. Database connection will be activated when Supabase is connected.",demo:true});const {createClient}=await import("@supabase/supabase-js");const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const {error}=await db.from("bookings").insert({tour_slug:body.tourSlug||null,name:body.name,email:body.email,dates:body.dates||null,travelers:Number(body.travelers||1),message:body.message||null,status:"new"});if(error)throw error;return NextResponse.json({message:"Booking request received. Our team will contact you."});}catch{return NextResponse.json({error:"Could not save booking."},{status:500})}}
