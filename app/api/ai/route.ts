import {NextResponse} from "next/server"; import OpenAI from "openai";
export async function POST(req:Request){try{const {prompt}=await req.json(); if(!prompt)return NextResponse.json({error:"Prompt is required"},{status:400}); if(!process.env.OPENAI_API_KEY)return NextResponse.json({answer:"AI is prepared but not connected yet. Add OPENAI_API_KEY in Vercel when we reach the connection step."}); const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); const model=process.env.OPENAI_MODEL||"gpt-5.6-luna"; const response=await client.responses.create({model,input:`You are Himalayan Adventures' travel research agent. Give practical, uncertainty-aware trip planning advice. Never invent closures, permits, prices, weather or availability. Distinguish facts from recommendations. User request: ${prompt}`}); return NextResponse.json({answer:response.output_text});}catch(e){return NextResponse.json({error:"AI request failed. Check your OpenAI configuration."},{status:500})}}
