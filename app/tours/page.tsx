import {tours} from "@/lib/tours"; import TourCard from "@/components/TourCard"; export default function Page(){return <section className="section"><div className="container"><div className="pagehero"><div className="eyebrow">TOUR DATABASE</div><h1>Himalayan tours</h1><p className="muted">Starter records ready for your real inventory.</p></div><div className="grid">{tours.map(t=><TourCard key={t.slug} tour={t}/>)}</div></div></section>}