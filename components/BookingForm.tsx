 "use client";
import {useState} from "react";
export default function BookingForm({tourSlug=""}:{tourSlug?:string}){const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const body=Object.fromEntries(f.entries());const r=await fetch("/api/bookings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const j=await r.json();setMsg(j.message||j.error||"Submitted");setBusy(false)}return <form className="card" onSubmit={submit}><input type="hidden" name="tourSlug" value={tourSlug}/><span className="pill">BOOKING REQUEST</span><h3>Start your journey</h3><div className="field"><label>Name</label><input name="name" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Preferred dates</label><input name="dates" placeholder="e.g. 5–18 October 2026"/></div><div className="field"><label>Travelers</label><input name="travelers" type="number" min="1" defaultValue="2"/></div><div className="field"><label>Message</label><textarea name="message" placeholder="Tell us about fitness, interests and anything important." /></div><button className="btn" disabled={busy}>{busy?"Sending…":"Send booking request"}</button>{msg&&<div className="notice" style={{marginTop:14}}>{msg}</div>}</form>}
