 "use client";
import {useState} from "react";
export default function AIAgent(){const [q,setQ]=useState("");const [a,setA]=useState("");const [loading,setLoading]=useState(false);async function ask(e:React.FormEvent){e.preventDefault();setLoading(true);setA("");try{const r=await fetch("/api/ai",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({prompt:q})});const j=await r.json();setA(j.answer||j.error||"No answer.");}catch{setA("Please try again.");}finally{setLoading(false)}}return <div className="card"><span className="pill">AI TRAVEL AGENT</span><h3>Tell me what kind of Himalayan journey you want.</h3><form onSubmit={ask}><div className="field"><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Example: I have 12 days, moderate fitness, love photography and want a quieter route in October." required/></div><button className="btn" disabled={loading}>{loading?"Researching…":"Research my trip"}</button></form>{a&&<div className="chat" style={{marginTop:16}}>{a}</div>}<p className="muted">AI recommendations are not a substitute for official safety notices, permits or local guides.</p></div>}
